Command to create a vault file "ansible-vault create group_vars/all/vault.yml" Enter this in the terminal. You'll be prompted to set a password twice, then specify the information you want to hide in the vault.yml file. Add the file with the password (which you entered twice in the previous step) to the GitLab project variables.
